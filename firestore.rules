rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the game collection
    match /game/{document=**} {
      allow read, write: if true;
    }
  }
}
